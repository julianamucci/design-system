{
  "name": "${{ values.projectName }}",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "${{ values.description }}",
  "scripts": {
    "validate:docs": "npx tsx scripts/validate-docs-consistency.ts",
    "dev": "npm run dev --prefix design-system-${{ values.stack }}",
    "storybook": "npm run storybook --prefix design-system-${{ values.stack }}",
    "build": "npm run build-storybook --prefix design-system-${{ values.stack }}"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
