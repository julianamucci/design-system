import { Meta } from '@storybook/addon-docs/blocks';
import { createElement, useEffect, useRef } from 'react';
import { createApp } from 'vue';
import { createPinia } from 'pinia';
import ThemeColorsDocs from './ThemeColorsDocs.vue';

<Meta
  title="Foundations/Cores e Temas"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export function ThemeColorsDocsPage() {
  const ref = useRef(null);
  useEffect(() => {
    if (!ref.current) return;
    const app = createApp(ThemeColorsDocs);
    app.use(createPinia());
    app.mount(ref.current);
    return () => {
      try { app.unmount(); } catch {}
      if (ref.current) ref.current.innerHTML = '';
    };
  }, []);
  return createElement('div', { ref, style: { flex: 1, height: '100%' } });
}

<ThemeColorsDocsPage />
