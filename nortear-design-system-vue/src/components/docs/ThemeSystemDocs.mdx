import { Meta } from '@storybook/addon-docs/blocks';
import { createElement, useEffect, useRef } from 'react';
import { createApp } from 'vue';
import { createPinia } from 'pinia';
import ThemeSystemDocs from './ThemeSystemDocs.vue';

<Meta
  title="Foundations/Sistema de Temas"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export function ThemeSystemDocsPage() {
  const ref = useRef(null);
  useEffect(() => {
    if (!ref.current) return;
    const app = createApp(ThemeSystemDocs);
    app.use(createPinia());
    app.mount(ref.current);
    return () => {
      try { app.unmount(); } catch {}
      if (ref.current) ref.current.innerHTML = '';
    };
  }, []);
  return createElement('div', { ref, style: { flex: 1, height: '100%' } });
}

<ThemeSystemDocsPage />
