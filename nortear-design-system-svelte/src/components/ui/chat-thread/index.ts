import Root, {
	type ChatMessage,
	type ChatRole,
	type ChatSource,
	type ChatThreadLabels,
	type ChatThreadProps,
	type ChatToolCall,
	type ToolCallState,
} from "./chat-thread.svelte";

export {
	Root,
	//
	Root as ChatThread,
	type ChatMessage,
	type ChatRole,
	type ChatSource,
	type ChatThreadLabels,
	type ChatThreadProps,
	type ChatToolCall,
	type ToolCallState,
};
