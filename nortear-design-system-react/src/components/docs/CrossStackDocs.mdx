{/* Unattached docs page — sem componente atrelado (Meta sem prop "of") */}
import { Meta } from '@storybook/addon-docs/blocks';
import { CrossStackDocs } from './CrossStackDocs';

<Meta
  title="Foundations/Divergências Cross-Stack"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

<CrossStackDocs />
