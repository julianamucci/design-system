import { FoundationPage } from './shared/FoundationPage';
import translations from '@shared/content/foundations/divergencias-cross-stack/translations.json';

export function CrossStackDocs() {
  return <FoundationPage slug="divergencias-cross-stack" translations={translations} />;
}
