import { Meta } from '@storybook/addon-docs/blocks';
import { mountAngularInReact } from '../../lib/mountAngularInReact';
import { NdsIconsDocs } from './IconsDocs';

<Meta
  title="Foundations/Icons"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export const IconsDocsPage = mountAngularInReact(NdsIconsDocs);

<IconsDocsPage />
