import { Meta } from '@storybook/addon-docs/blocks';
import { montarAngularEmReact } from '../../lib/montarAngularEmReact';
import { NdsIconsDocs } from './IconsDocs';

<Meta
  title="Foundations/Icons"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export const IconsDocsPage = montarAngularEmReact(NdsIconsDocs);

<IconsDocsPage />
