import { Meta } from '@storybook/addon-docs/blocks';
import { montarAngularEmReact } from '../../lib/montarAngularEmReact';
import { NdsThemeColorsDocs } from './ThemeColorsDocs';

<Meta
  title="Foundations/Colors and Themes"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export const ThemeColorsDocsPage = montarAngularEmReact(NdsThemeColorsDocs);

<ThemeColorsDocsPage />
