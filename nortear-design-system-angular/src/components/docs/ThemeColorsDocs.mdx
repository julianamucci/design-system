import { Meta } from '@storybook/addon-docs/blocks';
import { mountAngularInReact } from '../../lib/mountAngularInReact';
import { NdsThemeColorsDocs } from './ThemeColorsDocs';

<Meta
  title="Foundations/Colors and Themes"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export const ThemeColorsDocsPage = mountAngularInReact(NdsThemeColorsDocs);

<ThemeColorsDocsPage />
