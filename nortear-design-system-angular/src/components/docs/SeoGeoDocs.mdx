import { Meta } from '@storybook/addon-docs/blocks';
import { mountAngularInReact } from '../../lib/mountAngularInReact';
import { NdsSeoGeoDocs } from './SeoGeoDocs';

<Meta
  title="About/SEO and GEO"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export const SeoGeoDocsPage = mountAngularInReact(NdsSeoGeoDocs);

<SeoGeoDocsPage />
