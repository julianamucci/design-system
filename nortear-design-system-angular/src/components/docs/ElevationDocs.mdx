import { Meta } from '@storybook/addon-docs/blocks';
import { mountAngularInReact } from '../../lib/mountAngularInReact';
import { NdsElevationDocs } from './ElevationDocs';

<Meta
  title="Foundations/Elevation, Borders and Shadows"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export const ElevationDocsPage = mountAngularInReact(NdsElevationDocs);

<ElevationDocsPage />
