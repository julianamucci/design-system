import { Meta } from '@storybook/addon-docs/blocks';
import { montarAngularEmReact } from '../../lib/montarAngularEmReact';
import { NdsElevationDocs } from './ElevationDocs';

<Meta
  title="Foundations/Elevation, Borders and Shadows"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export const ElevationDocsPage = montarAngularEmReact(NdsElevationDocs);

<ElevationDocsPage />
