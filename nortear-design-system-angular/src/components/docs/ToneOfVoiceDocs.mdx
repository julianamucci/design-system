import { Meta } from '@storybook/addon-docs/blocks';
import { montarAngularEmReact } from '../../lib/montarAngularEmReact';
import { NdsToneOfVoiceDocs } from './ToneOfVoiceDocs';

<Meta
  title="About/Tone of Voice"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export const ToneOfVoiceDocsPage = montarAngularEmReact(NdsToneOfVoiceDocs);

<ToneOfVoiceDocsPage />
