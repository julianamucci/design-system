import { Meta } from '@storybook/addon-docs/blocks';
import { mountAngularInReact } from '../../lib/mountAngularInReact';
import { NdsSpacingDocs } from './SpacingDocs';

<Meta
  title="Foundations/Spacing"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export const SpacingDocsPage = mountAngularInReact(NdsSpacingDocs);

<SpacingDocsPage />
