import { Meta } from '@storybook/addon-docs/blocks';
import { montarAngularEmReact } from '../../lib/montarAngularEmReact';
import { NdsSpacingDocs } from './SpacingDocs';

<Meta
  title="Foundations/Spacing"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export const SpacingDocsPage = montarAngularEmReact(NdsSpacingDocs);

<SpacingDocsPage />
