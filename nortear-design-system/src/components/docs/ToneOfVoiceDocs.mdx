import { Meta } from '@storybook/addon-docs/blocks';
import { createElement, useEffect, useRef } from 'react';
import { createToneOfVoiceDocs } from './ToneOfVoiceDocs';

<Meta
  title="Foundations/Tom de Voz"
  parameters={{
    layout: 'fullscreen',
    options: { showPanel: false },
    docs: {
      codePanel: false,
      source: { code: null },
    },
  }}
/>

export function ToneOfVoiceDocsPage() {
  const ref = useRef(null);
  useEffect(() => {
    if (!ref.current) return;
    const el = createToneOfVoiceDocs();
    ref.current.appendChild(el);
    return () => {
      if (ref.current) ref.current.innerHTML = '';
    };
  }, []);
  return createElement('div', { ref, style: { flex: 1, width: '100%', height: '100%' } });
}

<ToneOfVoiceDocsPage />
